# Adult App — Next.js prototype

This repository contains a small Next.js (App Router) + TypeScript + Tailwind scaffold for the "Adult" hi-fi prototype.

Quick start

```bash
cd /Users/lonelyhope/repos/design2
npm install
npm run dev
```

Open http://localhost:3000

What I scaffolded

- Next.js app directory under `src/app`
- Tailwind/PostCSS config
- Basic `AppShell`, `BottomNav` and `cn` helper
- Home page with a prominent Starter Guide card

Next steps

1. Run `npm install` then `npm run dev`.
2. If you want I can add onboarding, guides, community and history pages next.
